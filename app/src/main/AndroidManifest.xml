<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kingwen.frescofragment">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <!--可以读取设备外部存储空间（内置SDcard和外置SDCard）的文件-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--允许挂载和反挂载文件系统可移动存储-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!--允许写入或删除SD卡-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".MyApplication.MyApplication"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".Activities.PubBookDetailActivity"></activity>
        <activity android:name=".Activities.SearchResultActivity"></activity>
        <activity android:name=".Activities.ViewAllInfoActivity"></activity>
        <activity android:name=".Activities.BookerDetailActivity"></activity>
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
        <activity android:name=".Activities.RegisterActivity"></activity>
        <activity android:name=".Activities.PublishActivity"> </activity>
        <activity android:name=".Activities.MyInforActivity"> </activity>
        <activity android:name=".Activities.ChangeMyInforActivity"></activity>

        <activity android:name=".Activities.ShowCompanyActivity"></activity>

    </application>


</manifest>
